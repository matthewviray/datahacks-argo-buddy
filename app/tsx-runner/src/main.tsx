import React from 'react';
import ReactDOM from 'react-dom/client';
import NoriApp from '../../NoriApp_2.jsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NoriApp />
  </React.StrictMode>
);
