import React from 'react';
import ReactDOM from 'react-dom/client';
import NoriApp from '../../argobuddy-app.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NoriApp />
  </React.StrictMode>
);
